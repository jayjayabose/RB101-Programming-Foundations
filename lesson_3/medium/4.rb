=begin
I prefer the second implementation.
The first implementation has a side effect (it mutates the array argument) and it returns the updated array
The seond implementation has no side effect, it only returns a new array.
=end